---
import '../styles/global.css'

interface Props {
  title?: string;
  description?: string;
}

const { title = 'The-Prompt-Shelf', description = 'Curated collection of AI coding rules and prompts.' } = Astro.props;
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="description" content={description} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:type" content="website" />
    <meta property="og:site_name" content="The-Prompt-Shelf" />
    <meta property="og:image" content="https://thepromptshelf.dev/og-default.svg" />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:site" content="@yuto_lab_note" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Space+Grotesk:wght@500;600;700&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet" />
    <title>{title}</title>
    <!-- Google Analytics -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-2535GY448D"></script>
    <script is:inline>
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());
      gtag('config', 'G-2535GY448D');
    </script>
  </head>
  <body class="min-h-screen flex flex-col antialiased selection:bg-white/10 selection:text-white">
    <!-- Nav -->
    <nav class="sticky top-0 z-50 border-b border-border-subtle bg-bg/80 backdrop-blur-xl">
      <div class="max-w-[1400px] mx-auto px-6 h-14 flex items-center justify-between">
        <a href="/" class="flex items-center gap-2.5 group">
          <span class="font-display font-semibold text-[15px] text-text-main group-hover:text-text-muted transition-colors">
            The-Prompt-Shelf
          </span>
        </a>
        <div class="flex items-center gap-5 text-[13px] text-text-faint">
          <a href="/rules" class="hover:text-text-muted transition-colors tracking-wide">Browse</a>
          <a href="/about" class="hover:text-text-muted transition-colors tracking-wide">About</a>
          <button id="lang-toggle" class="px-2 py-1 rounded text-[11px] font-mono border border-border-subtle hover:border-border-strong hover:text-text-muted transition-all tracking-wider uppercase">
            EN
          </button>
        </div>
      </div>
    </nav>

    <main class="flex-1">
      <slot />
    </main>

    <footer class="border-t border-border-subtle py-10 px-6 mt-auto">
      <div class="max-w-[1400px] mx-auto flex flex-col md:flex-row items-center justify-between gap-4 text-[13px] text-text-faint tracking-wide">
        <p>The-Prompt-Shelf</p>
        <div class="flex gap-6">
          <a href="/about" class="hover:text-text-muted transition-colors">About</a>
          <a href="https://x.com/yuto_lab_note" target="_blank" class="hover:text-text-muted transition-colors">Twitter</a>
        </div>
      </div>
    </footer>
    <script>
      const langBtn = document.getElementById('lang-toggle');
      let lang = localStorage.getItem('lang') || 'ja';

      function applyLang() {
        langBtn!.textContent = lang === 'ja' ? 'EN' : 'JA';
        document.querySelectorAll('[data-en][data-ja]').forEach(el => {
          (el as HTMLElement).textContent = (el as HTMLElement).dataset[lang] || '';
        });
      }

      applyLang();

      langBtn?.addEventListener('click', () => {
        lang = lang === 'ja' ? 'en' : 'ja';
        localStorage.setItem('lang', lang);
        applyLang();
        window.dispatchEvent(new Event('lang-changed'));
      });
    </script>
  </body>
</html>
